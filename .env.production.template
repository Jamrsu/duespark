# DueSpark Production Environment Template
# ⚠️  CRITICAL: Set these values in your production environment
# ⚠️  NEVER commit this file with real values to version control
# ⚠️  Use your cloud provider's secrets management (AWS Secrets Manager, etc.)

#################################################################
# DATABASE CONFIGURATION (PRODUCTION)
#################################################################
# Use your production database credentials
DATABASE_URL=postgresql+psycopg2://username:password@host:5432/duespark_prod
# OR use separate components:
# POSTGRES_DB=duespark_prod
# POSTGRES_USER=duespark_prod
# POSTGRES_PASSWORD=YOUR_SECURE_PRODUCTION_DB_PASSWORD

#################################################################
# SECURITY CONFIGURATION (CRITICAL)
#################################################################
# Generate unique production keys with: openssl rand -base64 32
# These MUST be different from development keys
SECRET_KEY=PRODUCTION_32_BYTE_SECRET_KEY_REQUIRED
ENCRYPTION_KEY=PRODUCTION_32_BYTE_ENCRYPTION_KEY_REQUIRED
JWT_SECRET_KEY=PRODUCTION_32_BYTE_JWT_SECRET_KEY_REQUIRED

#################################################################
# ADMIN CONFIGURATION (PRODUCTION)
#################################################################
ADMIN_EMAIL=admin@yourdomain.com
# Use a highly secure password
ADMIN_PASSWORD=HIGHLY_SECURE_PRODUCTION_ADMIN_PASSWORD

#################################################################
# STRIPE CONFIGURATION (LIVE MODE)
#################################################################
# Use your LIVE Stripe credentials
STRIPE_CLIENT_ID=ca_YOUR_LIVE_STRIPE_CLIENT_ID
STRIPE_SECRET_KEY=sk_live_YOUR_LIVE_STRIPE_SECRET_KEY
STRIPE_WEBHOOK_SECRET=whsec_YOUR_LIVE_WEBHOOK_SECRET
STRIPE_API_VERSION=2023-10-16
STRIPE_REDIRECT_URI=https://api.yourdomain.com/integrations/stripe/callback

#################################################################
# EMAIL CONFIGURATION (PRODUCTION)
#################################################################
EMAIL_PROVIDER=postmark
POSTMARK_SERVER_TOKEN=YOUR_PRODUCTION_POSTMARK_TOKEN
POSTMARK_MESSAGE_STREAM=outbound
MAIL_FROM=no-reply@yourdomain.com
MAIL_FROM_NAME=DueSpark
EMAIL_FROM="DueSpark <no-reply@yourdomain.com>"
MAIL_REPLY_TO=support@yourdomain.com

#################################################################
# APPLICATION CONFIGURATION (PRODUCTION)
#################################################################
ENVIRONMENT=production
# CRITICAL: Must be false in production
DEBUG=false
PUBLIC_BASE_URL=https://api.yourdomain.com
API_V1_STR=/api/v1

# CORS for production - replace with your actual domains
BACKEND_CORS_ORIGINS=["https://app.yourdomain.com", "https://yourdomain.com"]

#################################################################
# FRONTEND CONFIGURATION (PRODUCTION)
#################################################################
VITE_API_BASE_URL=https://api.yourdomain.com
VITE_APP_URL=https://app.yourdomain.com
VITE_APP_NAME=DueSpark
VITE_APP_VERSION=1.0.0
VITE_APP_ENV=production

# Production feature flags
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_DEBUG=false
VITE_ENABLE_MOCK_DATA=false
VITE_LOG_LEVEL=error

# External services (production)
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_YOUR_STRIPE_PUBLISHABLE_KEY
VITE_SENTRY_DSN=https://YOUR_SENTRY_DSN@sentry.io/PROJECT_ID

#################################################################
# OPTIONAL: MONITORING & ANALYTICS
#################################################################
# SENTRY_DSN=https://YOUR_SENTRY_DSN@sentry.io/PROJECT_ID
# REDIS_URL=redis://your-production-redis:6379
# ELASTICSEARCH_URL=https://your-elasticsearch-cluster
# NEW_RELIC_LICENSE_KEY=your_new_relic_key